<!DOCTYPE html>
<html lang="it">
<head>
	<title>Amministrazione certificati</title>
	<link rel="stylesheet" href="/css/admin_panel.css">
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
	<script src="/js/admin_certificates_script.js" type="text/javascript"></script>
</head>
<body>
	<h4>In questa sezione puoi inserire i certificati o l'abbonamento</h4>
	<h4>Inserisci certificato</h4>
	<input id="certificate_input" type="date">
	<select id="certificate_select">
		<% if (users){ %>
			<% for (var i in users) { %>
				<option value="<%= users[i].id %>_<%= users[i].name %>_<%= users[i].surname %>"><%= users[i].name %> <%= users[i].surname %></option>
			<% } %>
		<% } %>
	</select>
	<input id="certificate_btn" type="button" value="Inserisci certificato">

	<br>
	<h4>Inserisci abbonamento</h4>
	<input id="subscription_input" type="date">
	<select id="subscription_select">
		<% if (users) { %>
			<% for (var i in users) { %>
				<option value="<%= users[i].id %>_<%= users[i].name %>_<%= users[i].surname %>"><%= users[i].name %> <%= users[i].surname %></option>
			<% } %>
		<% } %>
	</select>
	<input id="subscription_btn" type="button" value="Inserisci abbonamento">

	<h4>In questa sezione potrai vedere abbonamenti e certificati per utente</h4>
	<table>
		<thead>
			<tr>
				<th>Nome</th>
				<th>Cognome</th>
				<th>Certificato</th>
				<th>Abbonamento</th>
				<th>Id utente</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<% if (certificates) { %>
				<% for (var i in certificates) { %>
					<tr>
						<% for (var j in certificates[i]) { %>
							<% if(certificates[i][j]) { %>
								<td><%= certificates[i][j] %></td>
							<% } else{ %>
								<td>Non inserito</td>
							<% } %>
						<% } %>
						<td><input id="<%= certificates[i].id %>_remove" class="remove-btn" type="button" value="Rimuovi certificati"></td>
					</tr>
				<% } %>
			<% } %>
		</tbody>
	</table>
</body>
</html>