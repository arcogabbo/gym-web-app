<!DOCTYPE html>
<html lang="it">
<head>
	<title>Pannello admin di <%= user.name %> <%= user.surname %></title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="/css/admin_panel.css">
	<link rel="stylesheet" href="/css/materialize.min.css">
	<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
	<script src="/js/admin_panel_script.js" type="text/javascript"></script>
</head>
<body>

	<%- include ('bar.ejs') %>

	<div class="container">
	<h4 class="flow-text">In questa sezione potrai accettare gli utenti nuovi</h4>

	<table class="centered striped">
		<thead class="blue-grey darken-3">
			<tr>
				<th>Id utente</th>
				<th>Nome</th>
				<th>Cognome</th>
				<th></th>
			</tr>
		</thead>
		<tbody class="grey lighten-1">
			<% if (to_accept) { %>
			<% for(var i in to_accept) { %>
				<tr>
					<% for (var j in to_accept[i]) { %>
						<td><%= to_accept[i][j] %></td>
					<% } %>
					<td><button id="<%= to_accept[i].id %>" class="accept-btn btn waves-effect waves-light deep-orange darken-3"><i class="material-icons">check</i></button></td>
				</tr>
			<% } %>
			<% } %>
		</tbody>
	</table>
	</div>

	<%- include ('admin_button.ejs') %>

	<script type="text/javascript" src="/js/materialize.min.js"></script>
</body>
</html>